import { HighlightStatusDirective } from "./highlight-status";

describe('HighlightStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightStatusDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
